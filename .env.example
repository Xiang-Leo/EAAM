# Frontend Environment Variables
# ------------------------------
# Keep this empty for the default Docker deployment.
# The browser will call the frontend origin, for example:
#   http://<YOUR_SERVER_IP>:9039/api/summary
# Next.js will proxy /api/* to the backend container internally.

NEXT_PUBLIC_API_BASE_URL=
